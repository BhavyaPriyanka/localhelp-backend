apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: localhelp
spec:
  selector:
    app: backend
    purpose: deployment
    tier: backend-app
    project: localhelp
  ports:
  - protocol: TCP
    port: 8080 #service-port
    targetPort: 8080 #target-port